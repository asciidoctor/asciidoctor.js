{
  "name": "@asciidoctor/root",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/asciidoctor"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "npm run test:root && npm run test --workspaces",
    "test:root": "node --test test/**.test.js",
    "lint": "biome check test tasks && npm run lint --workspaces",
    "format": "biome format --write test tasks && npm run format --workspaces",
    "release": "node tasks/release.js"
  },
  "devDependencies": {
    "downdoc": "1.0.2-stable",
    "semver": "7.7.4",
    "sinon": "~21.1"
  },
  "volta": {
    "node": "24.15.0"
  }
}
